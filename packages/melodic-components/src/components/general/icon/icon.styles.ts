import { css } from '@melodicdev/core';

export const iconStyles = () => css`
	:host {
		display: inline-flex;
		align-items: center;
		justify-content: center;
	}

	i {
		display: inline-flex;
		font-style: normal;
	}
`;
