/**
 * Tag dot indicator color.
 *
 * 'error' is the library-wide canonical name for the destructive/failure
 * color (matching alert/toast/badge/progress); 'danger' is a deprecated
 * alias kept for backwards compatibility and renders identically.
 */
export type TagDotColor = 'success' | 'warning' | 'danger' | 'error' | 'info' | 'primary' | 'secondary';
