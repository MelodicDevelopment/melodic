export type BadgeVariant = 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'error';

/** The sizes ml-badge actually styles (there is no xl badge). */
export type BadgeSize = 'xs' | 'sm' | 'md' | 'lg';
