const M = (n) => typeof n == "string" ? n : typeof n == "symbol" ? n.toString() : n.name;
function Rt(n) {
  return function(e, t, r) {
    e.params || (e.params = []), e.params[r] = { __injectionToken: M(n) };
  };
}
function te(n = {}) {
  return function(e) {
    const t = n.token ?? e, r = n.dependencies?.map((s) => M(s));
    k.bind(t, e, {
      singleton: n.singleton,
      dependencies: r,
      args: n.args
    });
  };
}
var H = class {
  constructor(n, e, t) {
    this._singleton = !0, this._dependencies = [], this._args = [], this._resolved = !1, this.key = n, this.token = e, this.type = t;
  }
  get isSingleton() {
    return this._singleton;
  }
  get isResolved() {
    return this._resolved;
  }
  get dependencies() {
    return this._dependencies;
  }
  get args() {
    return this._args;
  }
  get targetClass() {
    return this._class;
  }
  get factory() {
    return this._factory;
  }
  setClass(n) {
    return this._class = n, this;
  }
  setFactory(n) {
    return this._factory = n, this;
  }
  setSingleton(n) {
    return this._singleton = n, this;
  }
  withDependencies(n) {
    return this._dependencies = n.map((e) => M(e)), this;
  }
  withArgs(n) {
    return this._args = n, this;
  }
  getInstance() {
    return this._instance;
  }
  setInstance(n) {
    return this._instance = n, this._resolved = !0, this;
  }
  clearInstance() {
    return this._instance = void 0, this._resolved = !1, this;
  }
}, je = class {
  constructor() {
    this._bindings = /* @__PURE__ */ new Map(), this._constructionStack = /* @__PURE__ */ new Set();
  }
  bind(n, e, t) {
    let r, s, i;
    typeof e == "function" ? (r = n, s = e, i = t) : (r = n, s = n, i = e);
    const o = M(r), a = new H(o, r, "class");
    return a.setClass(s), i?.singleton !== void 0 && a.setSingleton(i.singleton), i?.dependencies && a.withDependencies(i.dependencies), i?.args && a.withArgs(i.args), this._bindings.set(o, a), a;
  }
  bindValue(n, e) {
    const t = M(n), r = new H(t, n, "value");
    return r.setInstance(e), r.setSingleton(!0), this._bindings.set(t, r), r;
  }
  bindFactory(n, e, t) {
    const r = M(n), s = new H(r, n, "factory");
    return s.setFactory(e), t?.singleton !== void 0 && s.setSingleton(t.singleton), this._bindings.set(r, s), s;
  }
  get(n) {
    const e = M(n), t = this._bindings.get(e);
    if (!t) throw new Error(`Dependency could not be found: ${e}`);
    return this.resolve(t, e);
  }
  has(n) {
    const e = M(n);
    return this._bindings.has(e);
  }
  getBinding(n) {
    const e = M(n);
    return this._bindings.get(e);
  }
  unbind(n) {
    const e = M(n);
    return this._bindings.delete(e);
  }
  clear() {
    this._bindings.clear();
  }
  resolve(n, e) {
    if (n.type === "value") return n.getInstance();
    const t = n.getInstance();
    if (t !== void 0 && n.isSingleton) return t;
    if (this._constructionStack.has(e)) {
      const r = Array.from(this._constructionStack).join(" -> ") + ` -> ${e}`;
      throw new Error(`Circular dependency detected: ${r}`);
    }
    this._constructionStack.add(e);
    try {
      let r;
      return n.type === "factory" ? r = n.factory() : r = this.construct(n, e), n.isSingleton && n.setInstance(r), r;
    } finally {
      this._constructionStack.delete(e);
    }
  }
  construct(n, e) {
    const t = n.targetClass;
    let r = [];
    const s = t.params;
    if (s && Array.isArray(s)) for (let i = 0; i < s.length; i++) {
      const o = s[i];
      if (o && typeof o == "object" && o.__injectionToken) {
        const a = o.__injectionToken, c = this._bindings.get(a);
        if (!c) throw new Error(`Dependency '${a}' not found (required by '${e}')`);
        r.push(this.resolve(c, a));
      } else r.push(void 0);
    }
    else if (n.dependencies.length > 0) for (const i of n.dependencies) {
      const o = this._bindings.get(i);
      if (!o) throw new Error(`Dependency '${i}' not found (required by '${e}')`);
      r.push(this.resolve(o, i));
    }
    return n.args.length > 0 && (r = r.concat(n.args)), Reflect.construct(t, r);
  }
};
const k = new je();
function O(n) {
  return function(e, t) {
    const r = `__service_${String(t)}`;
    e[r] = n, Object.defineProperty(e, t, {
      get() {
        const s = `__cached_${String(t)}`;
        return this[s] || (this[s] = k.get(n)), this[s];
      },
      enumerable: !0,
      configurable: !0
    });
  };
}
function B(n) {
  return Symbol(n);
}
async function xt(n = {}) {
  const e = n.devMode ?? !1, t = [];
  if (e && console.log("[Melodic] Bootstrap starting..."), n.onError) {
    const i = (a) => {
      n.onError(a.error, "error");
    }, o = (a) => {
      n.onError(a.reason, "unhandledrejection");
    };
    window.addEventListener("error", i), window.addEventListener("unhandledrejection", o), t.push({
      type: "error",
      handler: i
    }, {
      type: "unhandledrejection",
      handler: o
    });
  }
  if (n.onBefore && (e && console.log("[Melodic] Running onBefore hook..."), await n.onBefore()), n.providers) {
    for (const i of n.providers) i(k);
    e && console.log("[Melodic] Custom providers registered");
  }
  let r;
  if (n.rootComponent && n.target) {
    const i = typeof n.target == "string" ? document.querySelector(n.target) : n.target;
    if (!i) throw new Error(`[Melodic] Target element not found: ${n.target}`);
    if (!customElements.get(n.rootComponent)) throw new Error(`[Melodic] Component <${n.rootComponent}> is not registered. Make sure to import the component file before calling bootstrap().`);
    r = document.createElement(n.rootComponent), i.appendChild(r), e && console.log("[Melodic] Mounted root component", {
      component: n.rootComponent,
      target: n.target
    });
  }
  const s = {
    isDevMode: e,
    rootElement: r,
    get(i) {
      return k.get(i);
    },
    destroy() {
      for (const { type: i, handler: o } of t) window.removeEventListener(i, o);
      r?.parentNode && r.parentNode.removeChild(r), e && console.log("[Melodic] Application destroyed");
    }
  };
  return n.onReady && n.onReady(), e && console.log("[Melodic] Bootstrap complete"), k.bindValue("IMelodicApp", s), s;
}
function ae(n, e) {
  n.renderInto(e);
}
const xe = Symbol("melodic.signal"), Le = (n) => typeof n == "function" && xe in n;
var ce = "melodic-styles", Be = `style[${ce}], link[rel="stylesheet"][${ce}]`, ne = [], G = null;
const Ve = (n) => {
  if (Ke()) {
    le(n);
    return;
  }
  G || (G = Fe()), G.then(() => le(n));
};
var Fe = async () => {
  const n = document.querySelectorAll(Be);
  if (n.length !== 0)
    for (const e of n) {
      if (e instanceof HTMLStyleElement) {
        he(e.textContent ?? "");
        continue;
      }
      e instanceof HTMLLinkElement && (e.sheet || await new Promise((t) => {
        e.addEventListener("load", () => t(), { once: !0 });
      }), he(Array.from(e.sheet?.cssRules ?? []).map((t) => t.cssText).join(`
`)));
    }
}, le = (n) => {
  const e = n.adoptedStyleSheets ?? [], t = ne.filter((r) => !e.includes(r));
  t.length > 0 && (n.adoptedStyleSheets = [...e, ...t]);
}, he = (n) => {
  const e = n.trim();
  if (e.length > 0) {
    const t = new CSSStyleSheet();
    t.replaceSync(e), ne.push(t);
  }
}, Ke = () => ne.length > 0, Ee = null;
const de = (n) => {
  Ee = n;
}, Pe = () => Ee;
var He = class {
  constructor(n) {
    this.execute = n, this._dependencies = /* @__PURE__ */ new Set(), this._isRunning = !1, this._needsRerun = !1, this.run = () => {
      if (this._isRunning) {
        this._needsRerun = !0;
        return;
      }
      this._isRunning = !0;
      do {
        this._needsRerun = !1, this._dependencies.forEach((t) => {
          t.unsubscribe(this.run);
        }), this._dependencies.clear();
        const e = Pe();
        de(this), this.execute(), de(e);
      } while (this._needsRerun);
      this._isRunning = !1;
    };
  }
  addDependency(n) {
    this._dependencies.add(n);
  }
  destroy() {
    this._dependencies.forEach((n) => {
      n.unsubscribe(this.run);
    }), this._dependencies.clear();
  }
};
function S(n) {
  let e = n;
  const t = /* @__PURE__ */ new Set(), r = () => {
    [...t].forEach((i) => i(e));
  }, s = (() => {
    const i = Pe();
    return i && (i.addDependency(s), t.add(i.run)), e;
  });
  return s.set = (i) => {
    e !== i && (e = i, r());
  }, s.update = (i) => {
    s.set(i(e));
  }, s.subscribe = (i) => (t.add(i), () => t.delete(i)), s.unsubscribe = (i) => {
    t.delete(i);
  }, s.destroy = () => {
    t.clear();
  }, Object.defineProperty(s, xe, {
    value: !0,
    enumerable: !1,
    configurable: !1
  }), s;
}
function E(n) {
  const e = S(void 0), t = new He(() => {
    e.set(n());
  });
  t.run();
  const r = e.destroy;
  return e.destroy = () => {
    t.destroy(), r();
  }, e;
}
var Ge = {};
function We(n) {
  return Ge[n];
}
function ue(n, e) {
  return typeof n == "function" ? n(e ?? {}) : n;
}
var Ue = class {
  constructor(n, e = {}) {
    this.parent = null, this._validators = [], this._asyncValidators = [], this._touched = S(!1), this._dirty = S(!1), this._pending = S(!1), this._ownDisabled = S(!1), this._asyncValidationId = 0, this.value = S(n), this.errors = S(null), this._validators = e.validators ?? [], this._asyncValidators = e.asyncValidators ?? [], this._ownDisabled.set(e.disabled ?? !1), this.updateOn = e.updateOn ?? "change", this.messages = e.messages ?? {};
  }
  initializeAggregates() {
    this.dirty = E(() => this.computeDirty()), this.touched = E(() => this.computeTouched()), this.pending = E(() => this.computePending()), this.disabled = E(() => this.computeDisabled()), this.pristine = E(() => !this.dirty()), this.untouched = E(() => !this.touched()), this.enabled = E(() => !this.disabled()), this.invalid = E(() => this.errors() !== null || this.hasInvalidChild()), this.valid = E(() => !this.invalid() && !this.pending()), this.state = E(() => ({
      dirty: this.dirty(),
      touched: this.touched(),
      pristine: !this.dirty(),
      untouched: !this.touched(),
      valid: !this.invalid() && !this.pending(),
      invalid: this.invalid(),
      pending: this.pending(),
      disabled: this.disabled(),
      enabled: !this.disabled()
    }));
  }
  markAsTouched() {
    this._touched.set(!0), this.updateOn === "blur" && this.runValidation();
  }
  markAsUntouched() {
    this._touched.set(!1);
  }
  markAsDirty() {
    this._dirty.set(!0);
  }
  markAsPristine() {
    this._dirty.set(!1);
  }
  markAllAsTouched() {
    this.markAsTouched();
  }
  markAllAsUntouched() {
    this.markAsUntouched();
  }
  disable() {
    this._ownDisabled.set(!0);
  }
  enable() {
    this._ownDisabled.set(!1);
  }
  setValidators(n) {
    this._validators = n, this.runValidation();
  }
  addValidators(n) {
    this._validators = [...this._validators, ...n], this.runValidation();
  }
  removeValidators(n) {
    this._validators = this._validators.filter((e) => !n.includes(e)), this.runValidation();
  }
  setAsyncValidators(n) {
    this._asyncValidators = n, this.runValidation();
  }
  async validate() {
    await this.runValidation();
  }
  getError(n) {
    return this.errors()?.[n] ?? null;
  }
  hasError(n) {
    return this.errors()?.[n] !== void 0;
  }
  getErrorMessage(n) {
    const e = this.getError(n);
    if (!e) return "";
    const t = e.params, r = this.resolveFromChain(n);
    if (r !== void 0) return ue(r, t);
    const s = We(n);
    return s !== void 0 ? ue(s, t) : n;
  }
  getFirstErrorMessage() {
    const n = this.errors();
    if (!n) return "";
    const e = Object.keys(n);
    return e.length === 0 ? "" : this.getErrorMessage(e[0]);
  }
  resolveFromChain(n) {
    let e = this;
    for (; e !== null; ) {
      if (e.messages[n] !== void 0) return e.messages[n];
      e = e.parent;
    }
  }
  async runValidation() {
    const n = this.value();
    let e = null;
    for (const t of this._validators) {
      const r = t(n);
      r !== null && (e = {
        ...e ?? {},
        ...r
      });
    }
    if (e !== null) {
      this.errors.set(e);
      return;
    }
    if (this._asyncValidators.length > 0) {
      const t = ++this._asyncValidationId;
      this._pending.set(!0);
      try {
        const r = await Promise.all(this._asyncValidators.map((s) => s(n)));
        if (t !== this._asyncValidationId) return;
        for (const s of r) s !== null && (e = {
          ...e ?? {},
          ...s
        });
      } finally {
        t === this._asyncValidationId && this._pending.set(!1);
      }
    }
    this.errors.set(e);
  }
  computeDirty() {
    return this._dirty();
  }
  computeTouched() {
    return this._touched();
  }
  computePending() {
    return this._pending();
  }
  computeDisabled() {
    return this._ownDisabled();
  }
  hasInvalidChild() {
    return !1;
  }
  destroySignals() {
    this.value.destroy(), this.errors.destroy(), this._touched.destroy(), this._dirty.destroy(), this._pending.destroy(), this._ownDisabled.destroy(), this.dirty.destroy(), this.touched.destroy(), this.pristine.destroy(), this.untouched.destroy(), this.valid.destroy(), this.invalid.destroy(), this.pending.destroy(), this.disabled.destroy(), this.enabled.destroy(), this.state.destroy();
  }
}, ze = class extends HTMLElement {
  constructor(n, e) {
    super(), this._unsubscribers = [], this._renderScheduled = !1, this._booleanProperties = /* @__PURE__ */ new Set(), this._meta = n, this._component = e, this._component.elementRef = this, this._root = this.attachShadow({ mode: "open" }), Ve(this._root), this._style = this.renderStyles(), this.observe(), this._component.onInit && this._component.onInit();
  }
  get component() {
    return this._component;
  }
  async connectedCallback() {
    this.render(), this._component.onCreate !== void 0 && this._component.onCreate();
  }
  disconnectedCallback() {
    this._unsubscribers.forEach((e) => e()), this._unsubscribers = [];
    const n = this._root.__parts;
    if (n) {
      for (const e of n) if (e.actionCleanup) try {
        e.actionCleanup();
      } catch (t) {
        console.error("Action directive cleanup failed:", t);
      } finally {
        e.actionCleanup = void 0;
      }
    }
    this._component.onDestroy !== void 0 && this._component.onDestroy();
  }
  attributeChangedCallback(n, e, t) {
    const r = n.replace(/-([a-z])/g, (s, i) => i.toUpperCase());
    if (this._component[r] !== void 0) {
      let s = t;
      this._booleanProperties.has(r) && (s = t !== null && t !== "false"), this._component[r] = s;
    }
    this.scheduleRender(), this._component.onAttributeChange !== void 0 && this._component.onAttributeChange(n, e, t);
  }
  renderStyles() {
    const n = document.createElement("style");
    return this._meta.styles && ae(this._meta.styles(), n), this._root.appendChild(n);
  }
  render() {
    this._meta.template && (ae(this._meta.template(this._component, this.getAttributeValues()), this._root), this._style.parentNode !== this._root && this._root.appendChild(this._style)), this._component.onRender !== void 0 && this._component.onRender();
  }
  scheduleRender() {
    this._renderScheduled || (this._renderScheduled = !0, queueMicrotask(() => {
      this._renderScheduled = !1, this.isConnected && this.render();
    }));
  }
  observe() {
    const n = [], e = /* @__PURE__ */ new Set();
    let t = this._component;
    for (; t && t !== Object.prototype; ) {
      for (const s of Object.getOwnPropertyNames(t)) e.has(s) || (e.add(s), n.push(s));
      t = Object.getPrototypeOf(t);
    }
    const r = n.filter((s) => {
      const i = this._component[s];
      return s.startsWith("_") ? !1 : Le(i) ? (this.subscribeToSignal(i), !1) : i instanceof Ue ? (this.subscribeToSignal(i.state), !1) : typeof i == "function" ? !1 : s !== "elementRef" && s !== "constructor";
    });
    for (const s of r) {
      const i = this.getPropertyDescriptor(this._component, s), o = Object.getOwnPropertyDescriptor(this, s)?.value;
      let a = o === void 0 ? this._component[s] : o;
      typeof a == "boolean" && this._booleanProperties.add(s);
      let c = () => a, h = (d) => {
        a !== d && (this._component.onPropertyChange?.(s, a, d), a = d, this.scheduleRender());
      };
      if (i?.get) {
        const d = i.get;
        c = () => d.call(this._component) ?? a;
      }
      if (i?.set) {
        const d = i.set, u = h;
        h = (l) => {
          d.call(this._component, l), u(l);
        };
      }
      Object.defineProperty(this._component, s, {
        get: c,
        set: h,
        enumerable: !0,
        configurable: !0
      }), Object.defineProperty(this, s, {
        get: c,
        set: h,
        enumerable: !0,
        configurable: !0
      });
    }
  }
  getPropertyDescriptor(n, e) {
    let t = n;
    for (; t && t !== Object.prototype; ) {
      const r = Object.getOwnPropertyDescriptor(t, e);
      if (r) return r;
      t = Object.getPrototypeOf(t);
    }
  }
  getAttributeValues() {
    const n = {};
    return this.getAttributeNames().forEach((e) => {
      n[e] = this.getAttribute(e) ?? "";
    }), n;
  }
  subscribeToSignal(n) {
    const e = n.subscribe(() => this.scheduleRender());
    this._unsubscribers.push(e);
  }
};
function Me(n) {
  return function(e) {
    if (customElements.get(n.selector) === void 0) {
      const t = class extends ze {
        constructor() {
          const s = [], i = e.params;
          if (i && Array.isArray(i)) for (const o of i) {
            const a = i[o];
            a && typeof a == "object" && a.__injectionToken ? s.push(k.get(a.__injectionToken)) : s.push(void 0);
          }
          super(n, Reflect.construct(e, s));
        }
        static #e = this.observedAttributes = n.attributes ?? [];
      }, r = e;
      r.selector = n.selector, customElements.define(n.selector, t);
    }
  };
}
function Xe() {
  return "prod";
}
const Je = Xe();
function Se(n, e) {
  const t = { ...n };
  for (const r of Object.keys(e)) {
    const s = t[r], i = e[r];
    i !== null && typeof i == "object" && !Array.isArray(i) && s !== null && typeof s == "object" && !Array.isArray(s) ? t[r] = Se(s, i) : t[r] = i;
  }
  return t;
}
function Et(n) {
  const e = n[Je], t = {
    ...n.base,
    ...e
  };
  return n.extends ? Se(n.extends, t) : t;
}
const Qe = B("APP_CONFIG");
function Pt(n) {
  return (e) => {
    e.bindValue(Qe, n);
  };
}
var re = class ke extends Error {
  constructor(e, t, r) {
    super(e), this.config = t, this.code = r, this.name = "HttpBaseError", Object.setPrototypeOf(this, ke.prototype);
  }
}, fe = class Ae extends re {
  constructor(e, t, r) {
    super(e, r, `HTTP_${t.status}`), this.response = t, this.name = "HttpError", Object.setPrototypeOf(this, Ae.prototype);
  }
}, Ze = class Ne extends re {
  constructor(e, t) {
    super(e, t, "NETWORK_ERROR"), this.name = "NetworkError", Object.setPrototypeOf(this, Ne.prototype);
  }
}, Ye = class Te extends re {
  constructor(e, t) {
    super(e, t, "ABORTED"), this.name = "AbortError", Object.setPrototypeOf(this, Te.prototype);
  }
}, et = class {
  constructor() {
    this._pendingRequests = /* @__PURE__ */ new Map();
  }
  generateRequestKey(n, e, t) {
    let r = `${n}:${e}`;
    return t && (r += `:${this.hashBody(t)}`), r;
  }
  hasPendingRequest(n) {
    return this._pendingRequests.has(n);
  }
  getPendingRequest(n) {
    const e = this._pendingRequests.get(n);
    return e ? e.promise : null;
  }
  addPendingRequest(n, e) {
    const t = this.generateRequestKey(n.method || "GET", n.url || "", n.body);
    this._pendingRequests.set(t, {
      promise: e,
      abortController: n.abortController
    }), e.finally(() => {
      this.removePendingRequest(t);
    });
  }
  cancelPendingRequest(n, e) {
    const t = this._pendingRequests.get(n);
    t && (t.abortController.abort(e), this._pendingRequests.delete(n));
  }
  cancelAllRequests(n) {
    this._pendingRequests.forEach((e) => {
      e.abortController.abort(n);
    }), this._pendingRequests.clear();
  }
  removePendingRequest(n) {
    this._pendingRequests.get(n) && this._pendingRequests.delete(n);
  }
  hashBody(n) {
    let e;
    return typeof n == "string" ? e = n : n instanceof FormData ? e = "[FormData]" : n instanceof Blob ? e = `[Blob:${n.size}]` : n instanceof ArrayBuffer ? e = `[ArrayBuffer:${n.byteLength}]` : n instanceof URLSearchParams ? e = n.toString() : typeof n == "object" && n !== null ? e = JSON.stringify(n) : e = String(n), this.hashCode(e).toString();
  }
  hashCode(n) {
    let e = 0;
    for (let t = 0; t < n.length; t++) {
      const r = n.charCodeAt(t);
      e = (e << 5) - e + r, e = e & e;
    }
    return e;
  }
}, pe = class {
  constructor(n) {
    this._requestManager = new et(), this._interceptors = {
      request: [],
      response: []
    }, this.interceptors = {
      request: (e) => {
        this._interceptors.request.push(e);
      },
      response: (e) => {
        this._interceptors.response.push(e);
      }
    }, this._clientConfig = {
      defaultHeaders: {},
      ...n
    };
  }
  async get(n, e) {
    return this.internalRequest({
      method: "GET",
      ...e,
      url: n,
      deduplicate: e?.deduplicate ?? !0
    });
  }
  async post(n, e, t) {
    return this.internalRequest({
      method: "POST",
      ...t,
      url: n,
      body: e
    });
  }
  async put(n, e, t) {
    return this.internalRequest({
      method: "PUT",
      ...t,
      url: n,
      body: e
    });
  }
  async patch(n, e, t) {
    return this.internalRequest({
      method: "PATCH",
      ...t,
      url: n,
      body: e
    });
  }
  async delete(n, e) {
    return this.internalRequest({
      method: "DELETE",
      ...e,
      url: n
    });
  }
  async internalRequest(n) {
    let e = this.mergeConfig(n);
    if (e = await this.executeRequestInterceptors(e), e.cancel?.cancelled) {
      e.cancel.cancelReason && console.log("[HttpClient] Request cancelled:", e.cancel.cancelReason);
      let r = {
        data: null,
        status: 0,
        statusText: "Request Cancelled",
        headers: new Headers(),
        config: e
      };
      return e.cancel.cancelledResponse && (r = {
        ...r,
        ...e.cancel.cancelledResponse
      }), Promise.resolve(r);
    }
    if (e.body instanceof FormData) {
      const r = { ...e.headers };
      delete r["Content-Type"], delete r["content-type"], e.headers = r;
    }
    e.abortController === void 0 && (e.abortController = new AbortController());
    let t = await this.executeRequest(e);
    return t = await this.executeResponseInterceptors(t), t;
  }
  async executeRequest(n) {
    if (n.deduplicate === !0) {
      const t = this._requestManager.generateRequestKey(n.method, n.url, n.body);
      if (this._requestManager.hasPendingRequest(t)) return this._requestManager.getPendingRequest(t);
    }
    const e = fetch(n.url, {
      method: n.method,
      headers: n.headers,
      body: this.prepareBody(n.body),
      credentials: n.credentials,
      mode: n.mode,
      signal: n.abortController?.signal
    }).then(async (t) => {
      const r = {
        data: await this.parseResponse(t, n.onProgress),
        status: t.status,
        statusText: t.statusText,
        headers: t.headers,
        config: n
      };
      if (!t.ok) throw new fe(`HTTP Error: ${t.status} ${t.statusText}`, r, n);
      return r;
    }).catch((t) => {
      throw t instanceof fe ? t : t instanceof Error && t.name === "AbortError" ? new Ye("Request aborted", n) : new Ze((t instanceof Error ? t.message : "Network error") || "Network error", n);
    });
    return n.deduplicate === !0 && this._requestManager.addPendingRequest(n, e), await e;
  }
  async executeRequestInterceptors(n) {
    for (const e of this._interceptors.request) try {
      if (n = await e.intercept(n), n.cancel?.cancelled) break;
    } catch (t) {
      throw e.error && await e.error(t), t;
    }
    return n;
  }
  async executeResponseInterceptors(n) {
    for (const e of this._interceptors.response) try {
      n = await e.intercept(n);
    } catch (t) {
      throw e.error && await e.error(t), t;
    }
    return n;
  }
  mergeConfig(n) {
    return {
      ...this._clientConfig,
      ...n,
      headers: {
        ...this._clientConfig.defaultHeaders,
        ...n.headers
      },
      url: this.buildUrl(n.url ?? "", n.params)
    };
  }
  buildUrl(n, e) {
    let t = `${this._clientConfig.baseURL || ""}${n}`;
    if (e) {
      const r = Object.entries(e).map(([s, i]) => `${encodeURIComponent(s)}=${encodeURIComponent(String(i))}`).join("&");
      t += `${t.includes("?") ? "&" : "?"}${r}`;
    }
    return t;
  }
  prepareBody(n) {
    return n == null ? null : n instanceof FormData || n instanceof Blob || n instanceof ArrayBuffer || n instanceof URLSearchParams || n instanceof ReadableStream || typeof n == "string" ? n : JSON.stringify(n);
  }
  async parseResponse(n, e) {
    const t = n.headers.get("content-type") || "", r = parseInt(n.headers.get("content-length") || "0", 10);
    if (e && n.body && r > 0) {
      const s = n.body.getReader();
      let i = 0;
      const o = [];
      for (; ; ) {
        const { done: c, value: h } = await s.read();
        if (c) break;
        o.push(h), i += h.length, e({
          loaded: i,
          total: r,
          percentage: i / r * 100
        });
      }
      const a = new Blob(o);
      if (t.includes("application/json")) {
        const c = await a.text();
        return JSON.parse(c);
      }
      return a;
    }
    return t.includes("application/json") ? await n.json() : t.includes("text/") ? await n.text() : t.includes("application/octet-stream") || t.includes("image/") ? await n.blob() : await n.text();
  }
};
function Mt(n, e) {
  return (t) => {
    const r = new pe(n);
    t.bindValue(pe, r), e?.request && e.request.forEach((s) => {
      r.interceptors.request(s);
    }), e?.response && e.response.forEach((s) => {
      r.interceptors.response(s);
    });
  };
}
function St(n) {
  return { canActivate: n };
}
function kt(n) {
  return { canDeactivate: n };
}
function At(n) {
  return { resolve: n };
}
var U = class {
  constructor(n, e) {
    this._reEscape = /[-[\]{}()+?.,\\^$|#\s]/g, this._reParam = /([:*])(\w+)/g, this._names = [], this._isWildcard = !1, this._route = n, this._rules = e, this._isWildcard = n.includes("*");
    let t = this._route.replace(this._reEscape, "\\$&");
    t = t.replace(this._reParam, (r, s, i) => (this._names.push(i), s === ":" ? "([^/]*)" : "(.*)")), this._routeRegex = /* @__PURE__ */ new RegExp("^" + t + "$"), this._prefixRegex = /* @__PURE__ */ new RegExp("^" + t + "(?:/|$)");
  }
  parse(n) {
    let e = 0, t, r;
    const s = {}, i = n.match(this._routeRegex);
    if (!i) return null;
    for (; e < this._names.length; ) {
      if (t = this._names[e++], r = i[e], this._rules && t in this._rules && !this.validateRule(this._rules[t], r)) return null;
      s[t] = r;
    }
    return s;
  }
  parsePrefix(n) {
    if (this._route === "") return {
      params: {},
      matchedPath: "",
      remainingPath: n
    };
    const e = n.match(this._prefixRegex);
    if (!e) return null;
    const t = {};
    for (let s = 0; s < this._names.length; s++) {
      const i = this._names[s], o = e[s + 1];
      if (this._rules && i in this._rules && !this.validateRule(this._rules[i], o)) return null;
      t[i] = o;
    }
    const r = this.calculateMatchedPath(n);
    return {
      params: t,
      matchedPath: r,
      remainingPath: n.slice(r.length).replace(/^\//, "")
    };
  }
  stringify(n) {
    let e, t = this._route;
    for (const r in n)
      e = /* @__PURE__ */ new RegExp("[:*]" + r + "\\b"), t = t.replace(e, n[r]);
    return t.replace(this._reParam, "");
  }
  calculateMatchedPath(n) {
    if (this._isWildcard) return n;
    const e = this._route.split("/").filter(Boolean);
    return n.split("/").filter(Boolean).slice(0, e.length).join("/");
  }
  validateRule(n, e) {
    const t = Object.prototype.toString.call(n).charAt(8);
    return t === "R" ? n.test(e) : t === "F" ? n(e) : n === e;
  }
};
const tt = "melodic:route-context";
var Nt = class extends CustomEvent {
  constructor(n) {
    super(tt, {
      bubbles: !1,
      composed: !0,
      detail: n
    });
  }
};
function Ie(n, e, t, r, s) {
  for (const i of n) {
    const o = new U(i.path);
    if (i.redirectTo && i.path === e) return {
      matches: r,
      params: s,
      isExactMatch: !1,
      redirectTo: i.redirectTo
    };
    const a = o.parse(e);
    if (a !== null) {
      const c = t ? `${t}/${i.path}` : i.path, h = {
        route: i,
        params: a,
        matchedPath: i.path,
        remainingPath: "",
        fullPath: c,
        children: i.children
      };
      return Object.assign(s, a), r.push(h), {
        matches: r,
        params: s,
        isExactMatch: !0
      };
    }
    if (i.children || i.loadChildren) {
      const c = o.parsePrefix(e);
      if (c && c.params !== null) {
        const h = t ? `${t}/${c.matchedPath}` : c.matchedPath, d = {
          route: i,
          params: c.params,
          matchedPath: c.matchedPath,
          remainingPath: c.remainingPath,
          fullPath: h,
          children: i.children
        };
        return Object.assign(s, c.params), r.push(d), i.children && c.remainingPath ? Ie(i.children, c.remainingPath, h, r, s) : {
          matches: r,
          params: s,
          isExactMatch: c.remainingPath === ""
        };
      }
    }
  }
  return {
    matches: r,
    params: s,
    isExactMatch: !1
  };
}
function z(n, e, t = "") {
  const r = Ie(n, e.startsWith("/") ? e.slice(1) : e, t, [], {});
  return {
    matches: r.matches,
    params: r.params,
    isExactMatch: r.isExactMatch,
    redirectTo: r.redirectTo
  };
}
function nt(n, e) {
  for (const t of n) {
    if (t.name === e) return t;
    if (t.children) {
      const r = nt(t.children, e);
      if (r) return r;
    }
  }
  return null;
}
function rt(n, e, t = {}) {
  const r = [];
  function s(i, o) {
    for (const a of i) {
      if (a.name === o) {
        const c = new U(a.path);
        return r.push(c.stringify(t)), !0;
      }
      if (a.children) {
        const c = new U(a.path).stringify(t);
        if (s(a.children, o))
          return r.unshift(c), !0;
      }
    }
    return !1;
  }
  return s(n, e) ? "/" + r.filter(Boolean).join("/") : null;
}
function P(n, e, t, r) {
  var s = arguments.length, i = s < 3 ? e : r === null ? r = Object.getOwnPropertyDescriptor(e, t) : r, o;
  if (typeof Reflect == "object" && typeof Reflect.decorate == "function") i = Reflect.decorate(n, e, t, r);
  else for (var a = n.length - 1; a >= 0; a--) (o = n[a]) && (i = (s < 3 ? o(i) : s > 3 ? o(e, t, i) : o(e, t)) || i);
  return s > 3 && i && Object.defineProperty(e, t, i), i;
}
var X = class {
  constructor() {
    this._matchStack = [], this._contexts = /* @__PURE__ */ new Map(), this._currentMatchResult = null, this._resolvedData = /* @__PURE__ */ new Map();
  }
  setMatchResult(e) {
    this._currentMatchResult = e, this._matchStack = e.matches, this._contexts.clear();
    let t = "";
    const r = [], s = {};
    for (let i = 0; i < e.matches.length; i++) {
      const o = e.matches[i];
      r.push(o), Object.assign(s, o.params);
      const a = {
        depth: i,
        routes: o.children ?? [],
        currentMatch: o,
        ancestorMatches: [...r],
        params: { ...s },
        remainingPath: o.remainingPath,
        basePath: t,
        parent: i > 0 ? this._contexts.get(i - 1) : void 0
      };
      this._contexts.set(i, a), t = o.fullPath;
    }
  }
  setResolvedData(e, t) {
    this._resolvedData.set(e, t);
  }
  clearResolvedData() {
    this._resolvedData.clear();
  }
  getContextForDepth(e) {
    return this._contexts.get(e);
  }
  getChildRoutesForDepth(e) {
    const t = this._contexts.get(e - 1);
    return e === 0 ? [] : t?.currentMatch?.children ?? [];
  }
  getRemainingPathForDepth(e) {
    return e === 0 ? window.location.pathname : this._contexts.get(e - 1)?.remainingPath ?? "";
  }
  getParamsForDepth(e) {
    return this._contexts.get(e)?.params ?? {};
  }
  getCurrentParams() {
    return this._currentMatchResult?.params ?? {};
  }
  getMatchStack() {
    return [...this._matchStack];
  }
  getCurrentMatchResult() {
    return this._currentMatchResult;
  }
  getMergedRouteData(e) {
    const t = e ?? this._matchStack.length - 1, r = {};
    for (let s = 0; s <= t && s < this._matchStack.length; s++) {
      const i = this._matchStack[s];
      i.route.data && Object.assign(r, i.route.data);
    }
    return r;
  }
  getMergedResolvedData(e) {
    const t = e ?? this._matchStack.length - 1, r = {};
    for (let s = 0; s <= t; s++) {
      const i = this._resolvedData.get(s);
      i && Object.assign(r, i);
    }
    return r;
  }
  getResolvedDataForDepth(e) {
    return this._resolvedData.get(e);
  }
};
X = P([te()], X);
function T(n, e) {
  if (typeof Reflect == "object" && typeof Reflect.metadata == "function") return Reflect.metadata(n, e);
}
var se = (n, e, t, r) => new PopStateEvent("History", { state: {
  type: n,
  data: e,
  url: r,
  host: window.location.host,
  hostName: window.location.hostname,
  href: window.location.href,
  pathName: window.location.pathname,
  port: window.location.port,
  protocol: window.location.protocol,
  params: new URLSearchParams(window.location.search),
  title: t
} }), st = history.pushState;
history.pushState = (n, e, t) => {
  st.apply(history, [
    n,
    e,
    t
  ]);
  const r = new CustomEvent("NavigationEvent", { detail: se("push", n, e, t) });
  window.dispatchEvent(r);
};
var it = history.replaceState;
history.replaceState = (n, e, t) => {
  it.apply(history, [
    n,
    e,
    t
  ]);
  const r = new CustomEvent("NavigationEvent", { detail: se("replace", n, e, t) });
  window.dispatchEvent(r);
};
var A = class {
  constructor() {
    this._routes = [], this._currentMatches = [], this._resolversExecutedForPath = null, this._currentPath = `${window.location.pathname}${window.location.search}`, this._contextService = new X(), window.addEventListener("NavigationEvent", (e) => {
      this._route = e.detail.state;
    }), window.addEventListener("popstate", (e) => {
      this.handlePopState(e);
    });
  }
  setRoutes(e) {
    this._routes = e;
  }
  getRoutes() {
    return this._routes;
  }
  getContextService() {
    return this._contextService;
  }
  getRoute() {
    return this._route;
  }
  getParams() {
    return this._contextService.getCurrentParams();
  }
  getParam(e) {
    return this._contextService.getCurrentParams()[e];
  }
  getQueryParams() {
    return new URLSearchParams(window.location.search);
  }
  getCurrentMatches() {
    return [...this._currentMatches];
  }
  getRouteData(e) {
    return this._contextService.getMergedRouteData(e);
  }
  getResolvedData(e) {
    return this._contextService.getMergedResolvedData(e);
  }
  matchPath(e) {
    return z(this._routes, e);
  }
  setCurrentMatches(e) {
    this._currentMatches = e.matches, this._contextService.setMatchResult(e);
  }
  async runResolvers(e) {
    const t = `${window.location.pathname}${window.location.search}`;
    if (this._resolversExecutedForPath === t)
      return this._resolversExecutedForPath = null, { success: !0 };
    const r = await this.runResolversInternal(e);
    return this._resolversExecutedForPath = null, r;
  }
  async navigate(e, t = {}) {
    const { data: r, replace: s = !1, queryParams: i, skipGuards: o = !1, skipResolvers: a = !1, scrollToTop: c = !0 } = t;
    let h = e;
    if (i && Object.keys(i).length > 0 && (h = `${e}?${new URLSearchParams(i).toString()}`), !o && this._currentMatches.length > 0) {
      const u = await this.runDeactivationGuards(h);
      if (u !== !0)
        return typeof u == "string" ? this.navigate(u, {
          ...t,
          skipGuards: !0
        }) : {
          success: !1,
          error: "Navigation blocked by guard"
        };
    }
    const d = this.matchPath(e);
    if (d.redirectTo) return this.navigate(d.redirectTo, {
      ...t,
      replace: !0
    });
    if (!o && d.matches.length > 0) {
      const u = await this.runGuards(d);
      if (u !== !0)
        return typeof u == "string" ? this.navigate(u, {
          ...t,
          skipGuards: !0
        }) : {
          success: !1,
          error: "Navigation blocked by guard"
        };
    }
    if (!a && d.matches.length > 0) {
      const u = await this.runResolversInternal(d);
      if (!u.success) return {
        success: !1,
        error: u.error ?? "Navigation blocked by resolver"
      };
      this._resolversExecutedForPath = h;
    }
    if (this.setCurrentMatches(d), s ? history.replaceState(r, "", h) : history.pushState(r, "", h), this._currentPath = h, c) {
      const u = h.includes("#") ? h.split("#")[1] : null;
      if (u) {
        const l = document.getElementById(u);
        l && l.scrollIntoView();
      } else window.scrollTo(0, 0);
    }
    return {
      success: !0,
      url: h
    };
  }
  async navigateByName(e, t = {}, r = {}) {
    const s = rt(this._routes, e, t);
    return s ? this.navigate(s, r) : {
      success: !1,
      error: `Route with name '${e}' not found`
    };
  }
  replace(e, t) {
    history.replaceState(t, "", e), this._currentPath = `${window.location.pathname}${window.location.search}`;
  }
  back() {
    history.back();
  }
  forward() {
    history.forward();
  }
  go(e) {
    history.go(e);
  }
  async runDeactivationGuards(e) {
    for (const t of this._currentMatches) {
      const r = t.route.canDeactivate ?? [];
      for (const s of r) {
        const i = this.createGuardContext(t, {
          matches: this._currentMatches,
          params: this._contextService.getCurrentParams(),
          isExactMatch: !0
        });
        i.targetPath = e;
        const o = await this.executeGuard(s, "canDeactivate", i);
        if (o !== !0) return o;
      }
    }
    return !0;
  }
  async runGuards(e) {
    for (const t of e.matches) {
      const r = t.route.canActivate ?? [];
      for (const s of r) {
        const i = this.createGuardContext(t, e), o = await this.executeGuard(s, "canActivate", i);
        if (o !== !0) return o;
      }
    }
    return !0;
  }
  async executeGuard(e, t, r) {
    const s = e[t];
    if (!s) return !0;
    try {
      const i = s.call(e, r);
      return i instanceof Promise ? await i : i;
    } catch (i) {
      return console.error("Guard error:", i), !1;
    }
  }
  createGuardContext(e, t) {
    return {
      route: e,
      matchedRoutes: t.matches,
      params: t.params,
      queryParams: new URLSearchParams(window.location.search),
      targetPath: window.location.pathname,
      currentPath: window.location.pathname,
      data: e.route.data
    };
  }
  async runResolversInternal(e) {
    this._contextService.clearResolvedData();
    for (let t = 0; t < e.matches.length; t++) {
      const r = e.matches[t], s = r.route.resolve;
      if (!s) continue;
      const i = {}, o = this.createResolverContext(r, e);
      for (const [a, c] of Object.entries(s)) try {
        i[a] = await this.executeResolver(c, o);
      } catch (h) {
        return console.error(`Resolver '${a}' failed:`, h), {
          success: !1,
          error: `Resolver '${a}' failed: ${h instanceof Error ? h.message : String(h)}`
        };
      }
      this._contextService.setResolvedData(t, i);
    }
    return { success: !0 };
  }
  async handlePopState(e) {
    const t = `${window.location.pathname}${window.location.search}`, r = await this.runDeactivationGuards(t);
    if (r !== !0) {
      typeof r == "string" ? await this.navigate(r, {
        replace: !0,
        skipGuards: !0
      }) : history.replaceState(e.state, "", this._currentPath);
      return;
    }
    this._currentPath = t;
    const s = this.matchPath(window.location.pathname);
    this.setCurrentMatches(s);
    const i = new CustomEvent("NavigationEvent", { detail: se("push", e.state, "", window.location.pathname) });
    window.dispatchEvent(i);
  }
  async executeResolver(e, t) {
    const r = e.resolve(t);
    return r instanceof Promise ? await r : r;
  }
  createResolverContext(e, t) {
    return {
      route: e,
      matchedRoutes: t.matches,
      params: t.params,
      queryParams: new URLSearchParams(window.location.search),
      targetPath: window.location.pathname
    };
  }
};
A = P([te(), T("design:paramtypes", [])], A);
var D = /* @__PURE__ */ new Map(), De = (n) => {
  if (D.has(n)) return D.get(n);
  const e = n.toLowerCase();
  for (const [t, r] of D) if (t.toLowerCase() === e) return r;
};
function Oe(n, e) {
  D.set(n, e);
}
function ot(n) {
  return De(n);
}
function Dt(n) {
  return De(n) !== void 0;
}
function Ot(n) {
  return D.delete(n);
}
function $t() {
  return Array.from(D.keys());
}
function q(n) {
  return typeof n == "object" && n !== null && "__directive" in n;
}
var K = `m${Math.random().toString(36).slice(2, 9)}`, at = `<!--${K}-->`, ie = `__${K}_`, me = new RegExp(`${ie}(\\d+)__`, "g"), V = (n) => `${ie}${n}__`, j = /* @__PURE__ */ new Map(), ge = /* @__PURE__ */ new WeakMap();
function W(n) {
  let e = ge.get(n);
  return e === void 0 && (e = n.join(K), ge.set(n, e)), e;
}
var ct = class L {
  constructor(e, t) {
    this.strings = e, this.values = t;
  }
  /**
  * Optimized render for single-use containers (like repeat items).
  * Returns the rendered nodes directly.
  */
  renderOnce(e) {
    const t = W(this.strings), r = this.getTemplate(t), s = r.element.content.cloneNode(!0), i = this.prepareParts(s, r);
    return this.commit(i), e.appendChild(s), e.__parts = i, e.__templateKey = t, Array.from(e.childNodes);
  }
  renderInto(e) {
    const t = W(this.strings), { element: r } = this.getTemplate(t), s = e.__templateKey;
    if (s && s !== t) {
      const o = e.__parts;
      o && this.cleanupParts(o), delete e.__parts;
    }
    if (!e.__parts) {
      const o = r.content.cloneNode(!0), a = this.prepareParts(o, this.getTemplate(t));
      e.__parts = a, e.__templateKey = t, this.commit(a), e.textContent = "", e.appendChild(o);
      return;
    }
    e.__templateKey || (e.__templateKey = t);
    const i = e.__parts;
    this.commit(i);
  }
  getTemplate(e) {
    let t = j.get(e);
    if (t) return t;
    const r = [];
    let s = this.strings[0];
    const i = this.getAttributePreProcessor(r);
    let o = null, a = null;
    for (let p = 1; p < this.strings.length; p++) {
      const g = this.strings[p], w = p - 1, m = /([@.:?]?[\w:-]+)\s*=\s*["']?$/.exec(s), x = /([@.:?]?[\w:-]+)\s*=\s*(["'])([^"']*)$/.exec(s);
      let b = "___";
      if (o) s += V(w);
      else {
        const R = x?.[1]?.charAt(0), v = R !== void 0 && Object.keys(i).includes(R);
        if (x && !v)
          s += V(w), o = x[1], a = x[2];
        else {
          if (m) {
            b = "__";
            const I = m[1].charAt(0);
            Object.keys(i).includes(I) && (b = I);
          }
          if (b === "__" && m) {
            s += V(w), o = m[1];
            const I = /(["'])$/.exec(m[0]);
            a = I ? I[1] : null;
          } else s = i[b](w, s, m ? m[1] : void 0, m);
        }
      }
      s += g, o && (a ? g.includes(a) && (o = null, a = null) : /[\s>]/.test(g) && (o = null, a = null));
    }
    const c = document.createElement("template");
    c.innerHTML = s;
    const h = [];
    let d = 0;
    const u = [], l = /* @__PURE__ */ new Map(), f = /* @__PURE__ */ new Map(), y = /* @__PURE__ */ new Map(), _ = /* @__PURE__ */ new Map();
    for (const p of r) switch (p.type) {
      case "event":
        l.set(p.index, p);
        break;
      case "property":
        f.set(p.index, p);
        break;
      case "action":
        y.set(p.index, p);
        break;
      case "boolean-attribute":
        _.set(p.index, p);
        break;
      case "node":
        u.push(p);
        break;
      default:
        break;
    }
    const C = (p, g) => {
      if (p.nodeType === Node.COMMENT_NODE) {
        if (p.data === K) {
          const m = u[d++];
          m && h.push({
            path: [...g],
            type: "node",
            index: m.index
          });
        }
      } else if (p.nodeType === Node.ELEMENT_NODE) {
        const m = p;
        for (let x = m.attributes.length - 1; x >= 0; x--) {
          const b = m.attributes[x];
          if (b.name.startsWith("__event-")) {
            const R = parseInt(b.name.match(/__event-(\d+)__/)?.[1] || "0"), v = l.get(R);
            v && h.push({
              path: [...g],
              type: "event",
              index: v.index,
              name: v.name
            });
          } else if (b.name.startsWith("__prop-")) {
            const R = parseInt(b.name.match(/__prop-(\d+)__/)?.[1] || "0"), v = f.get(R);
            v && h.push({
              path: [...g],
              type: "property",
              index: v.index,
              name: v.name
            });
          } else if (b.name.startsWith("__action-")) {
            const R = parseInt(b.name.match(/__action-(\d+)__/)?.[1] || "0"), v = y.get(R);
            v && h.push({
              path: [...g],
              type: "action",
              index: v.index,
              name: v.name
            });
          } else if (b.name.startsWith("__bool-")) {
            const R = parseInt(b.name.match(/__bool-(\d+)__/)?.[1] || "0"), v = _.get(R);
            v && h.push({
              path: [...g],
              type: "boolean-attribute",
              index: v.index,
              name: v.name
            });
          } else if (b.name.startsWith(":")) h.push({
            path: [...g],
            type: "action",
            index: -1,
            name: b.name.slice(1),
            staticValue: b.value
          });
          else if (b.value.includes(ie)) {
            const R = this.parseAttributeValue(b.value);
            if (R) {
              const v = R.indices.length > 1 || R.strings.some((I) => I.length > 0);
              h.push({
                path: [...g],
                type: "attribute",
                index: R.indices[0],
                name: b.name,
                attributeStrings: v ? R.strings : void 0,
                attributeIndices: v ? R.indices : void 0
              });
            }
          }
        }
      }
      const w = p.childNodes;
      for (let m = 0; m < w.length; m++)
        g.push(m), C(w[m], g), g.pop();
    };
    if (C(c.content, []), t = {
      element: c,
      parts: r,
      partPaths: h
    }, j.size >= 500) {
      const p = j.keys().next().value;
      p && j.delete(p);
    }
    return j.set(e, t), t;
  }
  getAttributePreProcessor(e) {
    return {
      "@": (t, r, s, i) => (e.push({
        type: "event",
        index: t,
        name: s?.slice(1)
      }), r.slice(0, -(i?.[0].length ?? 0)) + `__event-${t}__=""`),
      ".": (t, r, s, i) => (e.push({
        type: "property",
        index: t,
        name: s?.slice(1)
      }), r.slice(0, -(i?.[0].length ?? 0)) + `__prop-${t}__=""`),
      ":": (t, r, s, i) => (e.push({
        type: "action",
        index: t,
        name: s?.slice(1)
      }), r.slice(0, -(i?.[0].length ?? 0)) + `__action-${t}__=""`),
      "?": (t, r, s, i) => (e.push({
        type: "boolean-attribute",
        index: t,
        name: s?.slice(1)
      }), r.slice(0, -(i?.[0].length ?? 0)) + `__bool-${t}__=""`),
      __: (t, r, s) => r + V(t),
      ___: (t, r) => (e.push({
        type: "node",
        index: t
      }), r + at)
    };
  }
  prepareParts(e, t) {
    const r = [], { partPaths: s } = t;
    for (const i of s) {
      let o = e;
      for (const a of i.path) o = o.childNodes[a];
      if (i.type === "node") {
        const a = document.createTextNode("");
        o.parentNode.replaceChild(a, o), r.push({
          type: "node",
          index: i.index,
          node: a
        });
      } else if (i.type === "event") {
        const a = o;
        a.removeAttribute(`__event-${i.index}__`), r.push({
          type: "event",
          index: i.index,
          name: i.name,
          node: a
        });
      } else if (i.type === "property") {
        const a = o;
        a.removeAttribute(`__prop-${i.index}__`), r.push({
          type: "property",
          index: i.index,
          name: i.name,
          node: a
        });
      } else if (i.type === "action") {
        const a = o;
        i.index >= 0 ? a.removeAttribute(`__action-${i.index}__`) : a.removeAttribute(`:${i.name}`), r.push({
          type: "action",
          index: i.index,
          name: i.name,
          node: a,
          staticValue: i.staticValue
        });
      } else if (i.type === "boolean-attribute") {
        const a = o;
        a.removeAttribute(`__bool-${i.index}__`), r.push({
          type: "boolean-attribute",
          index: i.index,
          name: i.name,
          node: a
        });
      } else if (i.type === "attribute") {
        const a = o;
        a.removeAttribute(i.name), r.push({
          type: "attribute",
          index: i.index,
          name: i.name,
          node: a,
          attributeStrings: i.attributeStrings,
          attributeIndices: i.attributeIndices
        });
      }
    }
    return r;
  }
  parseAttributeValue(e) {
    const t = [], r = [];
    let s = 0, i;
    for (me.lastIndex = 0; (i = me.exec(e)) !== null; )
      t.push(e.slice(s, i.index)), r.push(Number(i[1])), s = i.index + i[0].length;
    return r.length === 0 ? null : (t.push(e.slice(s)), {
      strings: t,
      indices: r
    });
  }
  /**
  * Sets up markers for a node part to enable complex content rendering
  */
  ensureMarkers(e) {
    if (e.startMarker) return;
    const t = e.node.parentNode;
    if (!t) return;
    const r = document.createComment("part-start"), s = document.createComment("part-end");
    t.insertBefore(r, e.node), t.insertBefore(s, e.node.nextSibling), e.startMarker = r, e.endMarker = s;
  }
  /**
  * Clears previously rendered nodes between markers
  */
  clearRenderedNodes(e) {
    if (!(!e.renderedNodes || e.renderedNodes.length === 0)) {
      for (const t of e.renderedNodes) t.parentNode?.removeChild(t);
      e.renderedNodes = [], e.arrayState = void 0, e.nestedContainer = void 0;
    }
  }
  /**
  * Clears DOM nodes created by a directive (e.g. repeat, when) when
  * switching away from a directive to a different renderable type.
  * Removes all nodes between the directive's markers and restores
  * part.node into the DOM so non-directive rendering can proceed.
  */
  clearDirectiveDOM(e) {
    const t = e.directiveState;
    if (!t) return;
    const r = t.startMarker, s = t.endMarker;
    if (r && s && r.parentNode) {
      const i = r.parentNode;
      let o = r.nextSibling;
      for (; o && o !== s; ) {
        const a = o.nextSibling;
        i.removeChild(o), o = a;
      }
      e.node && i.insertBefore(e.node, s), i.removeChild(r), i.removeChild(s);
    }
    e.directiveState = void 0;
  }
  cleanupParts(e) {
    for (const t of e) {
      if (t.actionCleanup) try {
        t.actionCleanup();
      } catch (r) {
        console.error("Action directive cleanup failed:", r);
      } finally {
        t.actionCleanup = void 0;
      }
      t.renderedNodes && t.renderedNodes.length > 0 && this.clearRenderedNodes(t);
    }
  }
  /**
  * Renders a nested TemplateResult into a node part.
  *
  * Uses a persistent DocumentFragment container to enable in-place updates
  * when the template structure hasn't changed (same tagged template literal).
  * This follows the same pattern as the `when` directive — the fragment holds
  * __parts/__templateKey after its children move to the real DOM, so subsequent
  * renderInto() calls diff and update existing nodes instead of recreating them.
  */
  renderNestedTemplate(e, t) {
    if (this.ensureMarkers(e), e.nestedContainer) {
      if (e.nestedContainer.__templateKey === W(t.strings)) {
        t.renderInto(e.nestedContainer);
        return;
      }
      const s = e.nestedContainer.__parts;
      s && this.cleanupParts(s);
    }
    this.clearRenderedNodes(e), e.node.textContent = "";
    const r = document.createDocumentFragment();
    t.renderInto(r), e.nestedContainer = r, e.renderedNodes = Array.from(r.childNodes), e.endMarker.parentNode.insertBefore(r, e.endMarker);
  }
  /**
  * Renders a DOM Node into a node part
  */
  renderNode(e, t) {
    this.ensureMarkers(e), this.clearRenderedNodes(e), e.node.textContent = "", e.renderedNodes = [t], e.endMarker.parentNode.insertBefore(t, e.endMarker);
  }
  /**
  * Renders an array of values into a node part
  */
  renderArray(e, t) {
    this.ensureMarkers(e), e.node.textContent = "";
    const r = e.endMarker.parentNode, s = this.getKeyedValues(t);
    if (s) {
      const o = e.arrayState ?? {
        items: /* @__PURE__ */ new Map(),
        keys: []
      }, a = /* @__PURE__ */ new Map(), c = [];
      for (const d of s) {
        const u = o.items.get(d.key);
        if (u)
          this.updateArrayItem(u, d.value, r, e.endMarker), a.set(d.key, u);
        else {
          const l = this.createArrayItem(d.value, r, e.endMarker);
          a.set(d.key, {
            key: d.key,
            value: d.value,
            container: l.container,
            nodes: l.nodes
          });
        }
        c.push(d.key);
      }
      for (const [d, u] of o.items.entries()) if (!a.has(d)) for (const l of u.nodes) l.parentNode?.removeChild(l);
      let h = e.startMarker.nextSibling;
      for (const d of c) {
        const u = a.get(d);
        for (const l of u.nodes) {
          if (l === h) {
            h = h?.nextSibling ?? null;
            continue;
          }
          r.insertBefore(l, h ?? e.endMarker);
        }
      }
      e.arrayState = {
        items: a,
        keys: c
      }, e.renderedNodes = c.flatMap((d) => a.get(d).nodes);
      return;
    }
    this.clearRenderedNodes(e);
    const i = [];
    for (const o of t) if (o instanceof L) {
      const a = document.createDocumentFragment();
      o.renderInto(a);
      const c = Array.from(a.childNodes);
      i.push(...c), r.insertBefore(a, e.endMarker);
    } else if (o instanceof Node)
      i.push(o), r.insertBefore(o, e.endMarker);
    else if (o != null) {
      const a = document.createTextNode(String(o));
      i.push(a), r.insertBefore(a, e.endMarker);
    }
    e.renderedNodes = i;
  }
  getKeyedValues(e) {
    if (e.length === 0) return null;
    const t = [];
    for (const r of e) if (r && typeof r == "object" && r.__keyed === !0) {
      const s = r;
      t.push({
        key: s.key,
        value: s.value
      });
    } else return null;
    return t;
  }
  createArrayItem(e, t, r) {
    const s = document.createDocumentFragment();
    e instanceof L ? e.renderInto(s) : e instanceof Node ? s.appendChild(e) : e != null && s.appendChild(document.createTextNode(String(e)));
    const i = Array.from(s.childNodes);
    return t.insertBefore(s, r), {
      container: s,
      nodes: i
    };
  }
  updateArrayItem(e, t, r, s) {
    if (t instanceof L) {
      t.renderInto(e.container), e.value = t, e.nodes = Array.from(e.container.childNodes);
      return;
    }
    if (t !== e.value) {
      for (const i of e.nodes) i.parentNode?.removeChild(i);
      e.container = document.createDocumentFragment(), t instanceof Node ? e.container.appendChild(t) : t != null && e.container.appendChild(document.createTextNode(String(t))), e.nodes = Array.from(e.container.childNodes), r.insertBefore(e.container, s), e.value = t;
    }
  }
  commit(e) {
    for (const t of e) {
      const r = this.values[t.index], s = t.type === "attribute" && t.attributeIndices && t.attributeStrings;
      if (!(!s && !q(r) && t.type !== "action" && t.previousValue === r)) {
        switch (t.type) {
          case "node":
            if (t.node) {
              const i = q(t.previousValue), o = q(r);
              i && !o && t.directiveState && this.clearDirectiveDOM(t), !i && o && this.clearRenderedNodes(t), o ? t.directiveState = r.render(t.node, t.directiveState) : r instanceof L ? this.renderNestedTemplate(t, r) : r instanceof Node ? this.renderNode(t, r) : Array.isArray(r) ? this.renderArray(t, r) : (this.clearRenderedNodes(t), t.node.textContent = String(r ?? ""));
            }
            break;
          case "attribute":
            if (t.node && t.name) {
              const i = t.node;
              if (q(r)) t.directiveState = r.render(i, t.directiveState);
              else if (s) {
                const o = t.attributeStrings, a = t.attributeIndices;
                let c = o[0] ?? "";
                for (let h = 0; h < a.length; h++) {
                  const d = this.values[a[h]];
                  c += `${d ?? ""}${o[h + 1] ?? ""}`;
                }
                if (t.previousValue === c) break;
                c === "" && o.every((h) => h === "") ? i.removeAttribute(t.name) : i.setAttribute(t.name, c), t.previousValue = c;
                continue;
              } else r == null || r === !1 ? i.removeAttribute(t.name) : r === !0 ? i.setAttribute(t.name, "") : i.setAttribute(t.name, String(r));
            }
            break;
          case "boolean-attribute":
            if (t.node && t.name) {
              const i = t.node;
              r ? i.setAttribute(t.name, "") : i.removeAttribute(t.name);
            }
            break;
          case "property":
            t.node && t.name && (q(r) ? t.directiveState = r.render(t.node, t.directiveState) : t.node[t.name] = r);
            break;
          case "event":
            if (t.node && t.name) {
              const i = t.node;
              if (t.previousValue === r) break;
              t.previousValue && typeof t.previousValue == "function" && i.removeEventListener(t.name, t.previousValue), typeof r == "function" && i.addEventListener(t.name, r);
            }
            break;
          case "action":
            if (t.node && t.name) {
              const i = t.node, o = t.index >= 0 ? r : t.staticValue;
              if (t.index >= 0 && t.previousValue === o || t.index < 0 && t.actionCleanup !== void 0) continue;
              t.actionCleanup && (t.actionCleanup(), t.actionCleanup = void 0);
              const a = ot(t.name);
              if (a) {
                const c = a(i, o, t.name);
                typeof c == "function" ? t.actionCleanup = c : t.actionCleanup = () => {
                };
              } else console.warn(`Attribute directive ':${t.name}' not found in registry`);
            }
            break;
          default:
            break;
        }
        t.previousValue = r;
      }
    }
  }
};
function oe(n, ...e) {
  return new ct(n, e);
}
const lt = oe;
var _e, ye = "melodic:outlet-register", J = class {
  constructor() {
    this._depth = 0, this._context = null, this._currentComponent = null, this._currentElement = null, this._childOutlets = /* @__PURE__ */ new Map(), this._parentOutlet = null, this._initialized = !1, this._navigationCleanup = null, this.routes = [], this.name = "primary";
  }
  onInit() {
    const e = () => this.onNavigate();
    window.addEventListener("NavigationEvent", e), this._navigationCleanup = () => window.removeEventListener("NavigationEvent", e), this.elementRef.addEventListener(ye, ((t) => {
      t.detail.outlet !== this && (t.stopPropagation(), this.registerChildOutlet(t.detail));
    }));
  }
  onCreate() {
    this.findParentOutlet(), queueMicrotask(() => {
      this._initialized = !0, this._depth === 0 && this.routes.length > 0 && this._router.setRoutes(this.routes), this._parentOutlet ? this.requestContextFromParent() : this.onNavigate();
    });
  }
  onDestroy() {
    this._navigationCleanup?.(), this._parentOutlet && this._parentOutlet.unregisterChildOutlet(this.name);
  }
  onPropertyChange(e) {
    e === "routes" && this._initialized && (this._depth === 0 && this._router.setRoutes(this.routes), this._currentComponent = null, this.onNavigate());
  }
  getDepth() {
    return this._depth;
  }
  getContext() {
    return this._context;
  }
  findParentOutlet() {
    let e = this.elementRef;
    for (; e; ) {
      const t = e.getRootNode();
      if (t instanceof ShadowRoot) {
        if (e = t.host, e.tagName.toLowerCase() !== "router-outlet") {
          const r = e.shadowRoot?.querySelector("router-outlet");
          if (r && r !== this.elementRef) {
            this._parentOutlet = r.component, this._depth = (this._parentOutlet?._depth ?? -1) + 1;
            return;
          }
        }
      } else {
        const r = e.closest?.("router-outlet");
        if (r && r !== this.elementRef) {
          this._parentOutlet = r.component, this._depth = (this._parentOutlet?._depth ?? -1) + 1;
          return;
        }
        break;
      }
    }
    this._depth = 0;
  }
  /**
  * Request context from parent outlet.
  */
  requestContextFromParent() {
    const e = new CustomEvent(ye, {
      bubbles: !0,
      composed: !0,
      detail: {
        outlet: this,
        callback: (t) => this.receiveContext(t)
      }
    });
    this.elementRef.dispatchEvent(e);
  }
  /**
  * Register a child outlet.
  */
  registerChildOutlet(e) {
    if (this._childOutlets.set(e.outlet.name, e.outlet), this._context?.currentMatch?.children) {
      const t = this.createChildContext();
      t && e.callback(t);
    }
  }
  unregisterChildOutlet(e) {
    this._childOutlets.delete(e);
  }
  receiveContext(e) {
    this._context = e, this.routes = e.routes, this.renderFromContext();
  }
  createChildContext() {
    if (!this._context?.currentMatch) return null;
    const e = this._context.currentMatch;
    return {
      depth: this._depth + 1,
      routes: e.children ?? [],
      currentMatch: void 0,
      ancestorMatches: [...this._context.ancestorMatches],
      params: { ...this._context.params },
      remainingPath: e.remainingPath,
      basePath: e.fullPath,
      parent: this._context
    };
  }
  onNavigate() {
    this._initialized && this._depth === 0 && this.matchAndRender(window.location.pathname);
  }
  /**
  * Match the current path and render (root outlet only).
  */
  async matchAndRender(e) {
    const t = this.routes.length > 0 ? this.routes : this._router.getRoutes();
    if (t.length === 0) return;
    const r = z(t, e);
    if (r.redirectTo) {
      window.location.pathname !== r.redirectTo && this._router.navigate(r.redirectTo, { replace: !0 });
      return;
    }
    if (r.matches.length > 0) {
      const s = await this._router.runGuards(r);
      if (s !== !0) {
        typeof s == "string" && this._router.navigate(s, {
          replace: !0,
          skipGuards: !0
        });
        return;
      }
    }
    if (r.matches.length > 0) {
      const s = await this._router.runResolvers(r);
      if (!s.success) {
        console.error("Resolver failed:", s.error), await this.render404();
        return;
      }
    }
    if (this._router.setCurrentMatches(r), r.matches.length > 0) {
      const s = r.matches[0];
      this._context = {
        depth: 0,
        routes: t,
        currentMatch: s,
        ancestorMatches: [s],
        params: s.params,
        remainingPath: s.remainingPath,
        basePath: "",
        parent: void 0
      }, await this.renderMatch(s, r);
    } else await this.render404();
  }
  async renderFromContext() {
    if (!this._context || this.routes.length === 0) return;
    const e = this._context.remainingPath, t = z(this.routes, e, this._context.basePath);
    if (t.redirectTo) {
      const r = this._context.basePath ? `/${this._context.basePath}/${t.redirectTo}`.replace(/\/+/g, "/") : t.redirectTo;
      window.location.pathname !== r && this._router.navigate(r, { replace: !0 });
      return;
    }
    if (t.matches.length > 0) {
      const r = t.matches[0];
      this._context = {
        ...this._context,
        currentMatch: r,
        ancestorMatches: [...this._context.ancestorMatches, r],
        params: {
          ...this._context.params,
          ...r.params
        }
      }, await this.renderMatch(r, t);
    } else await this.render404();
  }
  async renderMatch(e, t) {
    const r = e.route;
    if (r.component === this._currentComponent) {
      this.updateChildOutlets();
      return;
    }
    if (r.loadChildren && !e.children) try {
      const s = await r.loadChildren();
      e.children = s.routes, r.children = s.routes;
    } catch (s) {
      console.error("Failed to load child routes:", s), await this.render404();
      return;
    }
    if (r.loadComponent) try {
      await r.loadComponent();
    } catch (s) {
      console.error("Failed to load component:", s), await this.render404();
      return;
    }
    r.component && await this.renderComponent(r.component);
  }
  async renderComponent(e) {
    const t = this.elementRef.shadowRoot;
    if (!t) return;
    this._currentElement && (this._currentElement.remove(), this._currentElement = null), this._currentComponent = e;
    const r = document.createElement(e);
    r.__parentOutlet = this, t.appendChild(r), this._currentElement = r, queueMicrotask(() => this.updateChildOutlets());
  }
  updateChildOutlets() {
    const e = this.createChildContext();
    if (e)
      for (const [, t] of this._childOutlets) t.receiveContext(e);
  }
  async render404() {
    const e = this.routes.find((t) => t.path === "404" || t.path === "**");
    e?.component ? await this.renderComponent(e.component) : this._depth === 0 && this._router.navigate("/404", { replace: !0 });
  }
};
P([O(A), T("design:type", typeof (_e = typeof A < "u" && A) == "function" ? _e : Object)], J.prototype, "_router", void 0);
J = P([Me({
  selector: "router-outlet",
  template: () => oe`<slot></slot>`
})], J);
var ve, Q = class {
  constructor() {
    this._anchorElement = null, this._navigationCleanup = null, this.href = "", this.data = null, this.queryParams = {}, this.activeClass = "active", this.exactMatch = !1, this.replace = !1;
  }
  onCreate() {
    this._anchorElement = this.elementRef.shadowRoot?.querySelector("a") ?? null;
    const e = this.elementRef.getAttribute("href");
    e && (this.href = e);
    const t = this.elementRef.getAttribute("active-class");
    t && (this.activeClass = t), this.updateAnchorHref(), this.elementRef.addEventListener("click", (s) => {
      if (s.preventDefault(), s.ctrlKey || s.metaKey || s.shiftKey) {
        window.open(this.buildFullPath(), "_blank");
        return;
      }
      this.navigate();
    }, !1);
    const r = () => this.updateActiveState();
    window.addEventListener("NavigationEvent", r), this._navigationCleanup = () => window.removeEventListener("NavigationEvent", r), this.updateActiveState();
  }
  onDestroy() {
    this._navigationCleanup?.();
  }
  onAttributeChange(e, t, r) {
    e === "href" ? (this.href = r, this.updateAnchorHref(), this.updateActiveState()) : e === "active-class" && (this.activeClass = r, this.updateActiveState());
  }
  onPropertyChange(e) {
    (e === "href" || e === "queryParams") && (this.updateAnchorHref(), this.updateActiveState());
  }
  isActive() {
    const e = window.location.pathname, t = this.href.startsWith("/") ? this.href : `/${this.href}`;
    return this.exactMatch ? e === t : e.startsWith(t);
  }
  buildFullPath() {
    let e = this.href;
    if (this.queryParams && Object.keys(this.queryParams).length > 0) {
      const t = new URLSearchParams(this.queryParams);
      e = `${e}?${t.toString()}`;
    }
    return e;
  }
  updateAnchorHref() {
    this._anchorElement && (this._anchorElement.href = this.buildFullPath());
  }
  async navigate() {
    const e = {
      data: this.data,
      replace: this.replace,
      queryParams: this.queryParams
    };
    await this._router.navigate(this.href, e);
  }
  updateActiveState() {
    const e = window.location.pathname, t = this.href.startsWith("/") ? this.href : `/${this.href}`, r = e.replace(/\/$/, "") || "/", s = t.replace(/\/$/, "") || "/";
    let i;
    this.exactMatch ? i = r === s : i = r === s || r.startsWith(s + "/"), i ? (this.elementRef.classList.add(this.activeClass), this._anchorElement?.setAttribute("aria-current", "page")) : (this.elementRef.classList.remove(this.activeClass), this._anchorElement?.removeAttribute("aria-current"));
  }
};
P([O(A), T("design:type", typeof (ve = typeof A < "u" && A) == "function" ? ve : Object)], Q.prototype, "_router", void 0);
Q = P([Me({
  selector: "router-link",
  template: () => oe`<a part="link"><slot></slot></a>`,
  styles: () => lt`
		:host {
			display: inline-block;
			cursor: pointer;
		}
		a {
			color: inherit;
			text-decoration: inherit;
			font: inherit;
			display: block;
		}
	`,
  attributes: ["href", "active-class"]
})], Q);
function ht(n, e, t) {
  let r;
  if (typeof e == "string") r = { href: e };
  else if (e && typeof e == "object" && "href" in e) r = e;
  else {
    console.warn("routerLink: Invalid value. Expected string or { href: string, ... }");
    return;
  }
  const { href: s, activeClass: i = "active", exactMatch: o = !1, replace: a = !1, data: c = null, queryParams: h = {} } = r, d = k.get(A), u = () => {
    let _ = s;
    if (h && Object.keys(h).length > 0) {
      const C = new URLSearchParams(h);
      _ = `${_}?${C.toString()}`;
    }
    return _;
  };
  n.tagName.toLowerCase() === "a" && (n.href = u());
  const l = () => {
    const _ = window.location.pathname, C = s.startsWith("/") ? s : `/${s}`, p = _.replace(/\/$/, "") || "/", g = C.replace(/\/$/, "") || "/";
    let w;
    o ? w = p === g : w = p === g || p.startsWith(g + "/"), w ? (n.classList.add(i), n.tagName.toLowerCase() === "a" && n.setAttribute("aria-current", "page")) : (n.classList.remove(i), n.removeAttribute("aria-current")), n.setAttribute("router-link", "");
  }, f = (_) => {
    const C = _;
    if (C.ctrlKey || C.metaKey || C.shiftKey) {
      if (n.tagName.toLowerCase() === "a") return;
      window.open(u(), "_blank");
      return;
    }
    _.preventDefault();
    const p = {
      data: c,
      replace: a,
      queryParams: h
    };
    d.navigate(s, p);
  }, y = () => {
    l();
  };
  return n.addEventListener("click", f), window.addEventListener("NavigationEvent", y), l(), (() => {
    n.removeEventListener("click", f), window.removeEventListener("NavigationEvent", y);
  });
}
Oe("routerLink", ht);
const Lt = () => () => ({}), Bt = (n, e) => ((t) => ({
  type: n,
  payload: t ?? (e ? e() : void 0)
}));
function Vt(...n) {
  return { reducers: n };
}
const Ft = (n) => {
  const e = {};
  return Object.keys(n).forEach((t) => {
    e[t] = S(n[t]);
  }), e;
}, Kt = (n, e) => ({
  action: n(),
  reducer: e
}), Z = B("RX_INIT_STATE"), Y = B("RX_ACTION_PROVIDERS"), ee = B("RX_EFFECTS_PROVIDERS"), $e = B("RX_STATE_DEBUG");
var dt = class {
  constructor() {
    this._effects = [];
  }
  addEffect(n, e) {
    this._effects.push({
      actions: n,
      effect: e
    });
  }
  getEffects() {
    return this._effects;
  }
}, Ht = class extends dt {
  constructor(n, e = { reducers: [] }, t = !1) {
    super(), this._initState = n, this._reducerConfig = e, this._debug = t, this._state = S(n);
  }
  get state() {
    return this._state();
  }
  resetState() {
    this._state.set(this._initState);
  }
  select(n) {
    return E(() => n(this._state()));
  }
  dispatch(n) {
    this._debug && (console.log(`[ComponentState] Action: ${n.type}`), console.log("[ComponentState] Payload:", n.payload), console.log("[ComponentState] Before:", this._state()));
    const e = this._reducerConfig.reducers.find((t) => t.action.type === n.type);
    e && (this._state.update((t) => e.reducer(t, n)), this._debug && console.log("[ComponentState] After:", this._state())), this.executeEffects(n);
  }
  patchState(n) {
    this._state.update((e) => ({
      ...e,
      ...n
    }));
  }
  executeEffects(n) {
    this.getEffects().filter((e) => e.actions.some((t) => t().type === n.type)).forEach((e) => {
      e.effect(n).then((t) => {
        t !== void 0 && (Array.isArray(t) ? t : [t]).forEach((r) => this.dispatch(r));
      });
    });
  }
}, N = class {
  constructor() {
    this._debug && console.info("RX State Debugging: Enabled");
  }
  select(e, t) {
    return E(() => t(this._state[e]()));
  }
  logState() {
    console.log(this.getCurrentState());
  }
  dispatch(e, t) {
    const r = typeof e == "string" ? e : void 0, s = typeof e == "string" ? t : e;
    this._debug && (console.log(`Action: ${s.type}`), console.log("Payload:", s.payload), console.log("Current State:", this.getCurrentState())), r ? this.dispatchWithKey(r, s) : this.dispatchWithoutKey(s);
  }
  dispatchWithKey(e, t) {
    if (!this._reducerMap[e]) throw new Error(`Reducer not found for key: ${e}`);
    const r = this._reducerMap[e].reducers.find((s) => s.action.type === t.type);
    if (r !== void 0) {
      const s = r.reducer(this._state[e](), t);
      this._state[e].set(s), this._debug && console.log("New State:", this.getCurrentState());
    }
    this.getEffectsForAction(e, t).forEach((s) => {
      s.effect(t).then((i) => {
        i !== void 0 && (Array.isArray(i) || (i = [i]), i.forEach((o) => {
          this.dispatch(o);
        }));
      });
    });
  }
  dispatchWithoutKey(e) {
    const t = this.getReducerForAction(e);
    if (t !== void 0) {
      const s = t.actionReducer.reducer(this._state[t.key](), e);
      this._state[t.key].set(s), this._debug && console.log("New State:", this.getCurrentState());
    }
    const r = this.getEffectsForAction(e);
    r !== void 0 && r.actionEffects.forEach((s) => {
      s.effect(e).then((i) => {
        i !== void 0 && (Array.isArray(i) || (i = [i]), i.forEach((o) => {
          this.dispatch(o);
        }));
      });
    });
  }
  getReducerForAction(e) {
    const t = Object.keys(this._reducerMap);
    for (const r of t) {
      const s = (this._reducerMap[r]?.reducers || []).find((i) => i.action.type === e.type);
      if (s) return {
        key: r,
        actionReducer: s
      };
    }
  }
  getEffectsForAction(e, t) {
    return typeof e == "string" ? this.getEffectsForActionWithKey(e, t) : this.getEffectsForActionWithoutKey(e);
  }
  getEffectsForActionWithoutKey(e) {
    const t = Object.keys(this._reducerMap);
    for (const r of t) {
      const s = this._effectMap[r];
      if (s) {
        const i = k.get(s).getEffects().filter((o) => o.actions.some((a) => a().type === e.type));
        if (i.length > 0) return {
          key: r,
          actionEffects: i
        };
      }
    }
  }
  getEffectsForActionWithKey(e, t) {
    const r = this._effectMap[e];
    return r ? k.get(r).getEffects().filter((s) => s.actions.some((i) => i().type === t.type)) : [];
  }
  getCurrentState() {
    return Object.keys(this._state).reduce((e, t) => (e[t] = this._state[t](), e), {});
  }
};
P([O(Z), T("design:type", Object)], N.prototype, "_state", void 0);
P([O(Y), T("design:type", Object)], N.prototype, "_reducerMap", void 0);
P([O(ee), T("design:type", Object)], N.prototype, "_effectMap", void 0);
P([O($e), T("design:type", Boolean)], N.prototype, "_debug", void 0);
N = P([te(), T("design:paramtypes", [])], N);
function Wt(n, e, t, r = !1) {
  return (s) => {
    s.bindValue(Z, n), s.bindValue(Y, e), s.bindValue(ee, t), s.bindValue($e, r), s.bind(N, N, { dependencies: [
      Z,
      Y,
      ee
    ] });
  };
}
var be = /* @__PURE__ */ new WeakMap(), ut = class qe {
  constructor(e) {
    this._factory = null, this._hasEvents = !1, this._canCompile = !1, this.analyzeAndCompile(e);
  }
  static compile(e) {
    let t = be.get(e);
    return t || (t = new qe(e), be.set(e, t)), t;
  }
  /**
  * Check if this template can use the fast path
  */
  canUseFastPath() {
    return this._canCompile && !this._hasEvents;
  }
  /**
  * Create DOM nodes using the compiled factory
  */
  create(e) {
    return this._factory ? {
      nodes: [this._factory(e)],
      eventTargets: []
    } : {
      nodes: [],
      eventTargets: []
    };
  }
  /**
  * Directly create a single node - faster than create() for single-element templates
  */
  createDirect(e) {
    return this._factory ? this._factory(e) : null;
  }
  /**
  * Analyze template and generate optimized factory for simple cases
  *
  * Template strings format for: html`<li class="${val}">${text}</li>`
  * strings = ['<li class="', '">', '</li>']
  * values[0] goes between strings[0] and strings[1]
  * values[1] goes between strings[1] and strings[2]
  */
  analyzeAndCompile(e) {
    if (e.length < 2) return;
    for (const p of e) if (p.includes("@")) {
      this._hasEvents = !0;
      return;
    }
    let t = e[0];
    for (let p = 1; p < e.length; p++) t += `\${${p - 1}}` + e[p];
    const r = t.match(/^<([\w-]+)([^>]*)>(.*)$/s);
    if (!r) return;
    const [, s, i, o] = r, a = `</${s}>`;
    if (!o.endsWith(a)) return;
    const c = o.slice(0, -a.length), h = [], d = /([\w-]+)\s*=\s*(?:"([^"]*)"|'([^']*)'|\$\{(\d+)\})/g;
    let u;
    for (; (u = d.exec(i)) !== null; ) {
      const p = u[1], g = u[2] ?? u[3], w = u[4];
      if (w !== void 0) h.push({
        name: p,
        valueIndex: parseInt(w, 10)
      });
      else if (g !== void 0) {
        const m = g.match(/\$\{(\d+)\}/);
        m ? h.push({
          name: p,
          valueIndex: parseInt(m[1], 10)
        }) : h.push({
          name: p,
          valueIndex: null,
          staticValue: g
        });
      }
    }
    const l = [], f = c, y = /\$\{(\d+)\}/g;
    let _ = 0, C;
    for (y.lastIndex = 0; (C = y.exec(f)) !== null; )
      C.index > _ && l.push({ static: f.slice(_, C.index) }), l.push({ valueIndex: parseInt(C[1], 10) }), _ = C.index + C[0].length;
    _ < f.length && l.push({ static: f.slice(_) }), this._factory = (p) => {
      const g = document.createElement(s);
      for (const m of h) if (m.valueIndex !== null) {
        const x = p[m.valueIndex];
        x != null && x !== !1 && g.setAttribute(m.name, x === !0 ? "" : String(x));
      } else m.staticValue !== void 0 && g.setAttribute(m.name, m.staticValue);
      let w = "";
      for (const m of l) "static" in m ? w += m.static : w += p[m.valueIndex] ?? "";
      return g.textContent = w, g;
    }, this._canCompile = !0;
  }
};
function $(n) {
  return {
    __directive: !0,
    render: n
  };
}
function Ut(n, e, t) {
  return $((r, s) => {
    if (!s) {
      const i = r.parentNode;
      if (!i) throw new Error("repeat() directive: container must be attached to a parent node");
      const o = document.createComment("repeat-start"), a = document.createComment("repeat-end");
      i.replaceChild(o, r), i.insertBefore(a, o.nextSibling);
      const c = {
        keyToIndex: /* @__PURE__ */ new Map(),
        items: [],
        startMarker: o,
        endMarker: a
      };
      return we(n, e, t, c), c;
    }
    return we(n, e, t, s), s;
  });
}
function we(n, e, t, r) {
  const s = r.items, i = /* @__PURE__ */ new Map(), o = [];
  for (let l = 0; l < n.length; l++) {
    const f = e(n[l], l);
    i.set(f, l);
  }
  if (s.length === n.length) {
    let l = !0;
    for (let f = 0; f < n.length; f++) {
      const y = e(n[f], f);
      if (f >= s.length || s[f].key !== y) {
        l = !1;
        break;
      }
    }
    if (l) {
      for (let f = 0; f < n.length; f++) t(n[f], f).renderInto(s[f].container);
      return;
    }
  }
  if (r.useCompiledPath === void 0 && n.length > 0) {
    const l = t(n[0], 0), f = ut.compile(l.strings);
    r.useCompiledPath = f.canUseFastPath(), r.useCompiledPath && (r.compiledTemplate = f);
  }
  const a = /* @__PURE__ */ new Map(), c = /* @__PURE__ */ new Map();
  for (const l of s)
    a.set(l.key, l), c.set(l.key, c.size);
  for (let l = 0; l < n.length; l++) {
    const f = n[l], y = e(f, l);
    if (a.has(y)) {
      const _ = a.get(y);
      a.delete(y), t(f, l).renderInto(_.container), o.push({
        item: _,
        oldIndex: c.get(y) ?? -1,
        isNew: !1
      });
    } else {
      const _ = ft(f, l, y, t, r);
      o.push({
        item: _,
        oldIndex: -1,
        isNew: !0
      });
    }
  }
  for (const l of a.values()) gt(l);
  if (o.length === 0) {
    r.keyToIndex = i, r.items = [];
    return;
  }
  const h = _t(o), d = r.startMarker.parentElement;
  let u = r.endMarker;
  for (let l = o.length - 1; l >= 0; l--) {
    const f = o[l];
    f.isNew ? pt(f.item, d, u) : h.has(l) || mt(f.item, u), u = f.item.start;
  }
  r.keyToIndex = i, r.items = o.map((l) => l.item);
}
function ft(n, e, t, r, s) {
  const i = r(n, e);
  let o, a;
  if (s.useCompiledPath && s.compiledTemplate) {
    const c = s.compiledTemplate.createDirect(i.values);
    c ? (o = [c], a = document.createDocumentFragment(), a.appendChild(c)) : (a = document.createDocumentFragment(), o = i.renderOnce(a));
  } else
    a = document.createDocumentFragment(), o = i.renderOnce(a);
  return {
    key: t,
    value: n,
    container: a,
    nodes: o,
    start: document.createComment("repeat-item-start"),
    end: document.createComment("repeat-item-end")
  };
}
function pt(n, e, t) {
  const r = document.createDocumentFragment();
  r.appendChild(n.start);
  for (const s of n.nodes) r.appendChild(s);
  r.appendChild(n.end), e.insertBefore(r, t);
}
function mt(n, e) {
  const t = e.parentNode;
  if (!t) return;
  const r = document.createDocumentFragment();
  let s = n.start;
  const i = n.end;
  for (; s; ) {
    const o = s.nextSibling;
    if (r.appendChild(s), s === i) break;
    s = o;
  }
  t.insertBefore(r, e);
}
function gt(n) {
  let e = n.start;
  const t = n.end;
  for (; e; ) {
    const r = e.nextSibling;
    if (e.parentNode?.removeChild(e), e === t) break;
    e = r;
  }
}
function _t(n) {
  const e = [], t = [];
  for (let i = 0; i < n.length; i++) n[i].oldIndex >= 0 && (e.push(n[i].oldIndex), t.push(i));
  const r = yt(e), s = /* @__PURE__ */ new Set();
  for (const i of r) {
    const o = t[i];
    o !== void 0 && s.add(o);
  }
  return s;
}
function yt(n) {
  if (n.length === 0) return [];
  const e = new Array(n.length).fill(-1), t = new Array(n.length).fill(0);
  let r = 0;
  for (let o = 0; o < n.length; o++) {
    const a = n[o];
    let c = 0, h = r;
    for (; c < h; ) {
      const d = c + h >> 1;
      n[t[d]] < a ? c = d + 1 : h = d;
    }
    c > 0 && (e[o] = t[c - 1]), t[c] = o, c === r && r++;
  }
  const s = new Array(r);
  let i = t[r - 1];
  for (let o = r - 1; o >= 0; o--)
    s[o] = i, i = e[i];
  return s;
}
function zt(n, e, t) {
  return $((r, s) => {
    if (!s) {
      const i = r.parentNode;
      if (!i) throw new Error("repeatRaw() directive: container must be attached to a parent node");
      const o = document.createComment("repeat-raw-start"), a = document.createComment("repeat-raw-end");
      i.replaceChild(o, r), i.insertBefore(a, o.nextSibling);
      const c = {
        keyToItem: /* @__PURE__ */ new Map(),
        startMarker: o,
        endMarker: a
      }, h = document.createDocumentFragment();
      for (let d = 0; d < n.length; d++) {
        const u = n[d], l = e(u, d), f = t(u, d);
        c.keyToItem.set(l, {
          key: l,
          element: f
        }), h.appendChild(f);
      }
      return i.insertBefore(h, a), c;
    }
    return vt(n, e, t, s), s;
  });
}
function vt(n, e, t, r) {
  const s = r.keyToItem, i = /* @__PURE__ */ new Map(), o = r.startMarker.parentElement, a = r.endMarker;
  if (s.size === n.length) {
    let d = !0, u = 0;
    for (const [l] of s) {
      if (l !== e(n[u], u)) {
        d = !1;
        break;
      }
      u++;
    }
    if (d) {
      u = 0;
      for (const [l, { element: f }] of s) {
        const y = n[u], _ = t(y, u);
        f !== _ ? (f.replaceWith(_), i.set(l, {
          key: l,
          element: _
        })) : i.set(l, {
          key: l,
          element: f
        }), u++;
      }
      r.keyToItem = i;
      return;
    }
  }
  const c = document.createDocumentFragment(), h = /* @__PURE__ */ new Set();
  for (let d = 0; d < n.length; d++) {
    const u = n[d], l = e(u, d);
    h.add(l);
    const f = s.get(l);
    if (f) {
      const y = t(u, d);
      f.element !== y ? (i.set(l, {
        key: l,
        element: y
      }), c.appendChild(y)) : (i.set(l, f), c.appendChild(f.element));
    } else {
      const y = t(u, d);
      i.set(l, {
        key: l,
        element: y
      }), c.appendChild(y);
    }
  }
  for (const [d, { element: u }] of s) h.has(d) || u.remove();
  o.insertBefore(c, a), r.keyToItem = i;
}
function Xt(n, e, t) {
  return $((r, s) => {
    if (!s) {
      const i = r.parentNode;
      if (!i) throw new Error("when() directive: container must be attached to a parent node");
      const o = document.createComment("when-start"), a = document.createComment("when-end");
      i.replaceChild(o, r), i.insertBefore(a, o.nextSibling);
      const c = {
        condition: !1,
        template: e(),
        falseTemplate: t ? t() : null,
        container: null,
        startMarker: o,
        endMarker: a,
        nodes: []
      };
      return n ? F(c, !0) : c.falseTemplate && F(c, !1), c.condition = n, c;
    }
    if (!s.startMarker.parentNode) throw new Error("when() directive: markers were removed from DOM");
    if (n && !s.condition)
      Ce(s), s.template = e(), F(s, !0);
    else if (!n && s.condition)
      Ce(s), t && (s.falseTemplate = t(), F(s, !1));
    else if (n && s.condition) {
      const i = e();
      s.container && i.renderInto(s.container), s.template = i;
    } else if (!n && !s.condition && t) {
      const i = t();
      s.container && i.renderInto(s.container), s.falseTemplate = i;
    }
    return s.condition = n, s;
  });
}
function F(n, e) {
  const t = n.startMarker.parentNode;
  if (!t) throw new Error("when() directive: markers not in DOM");
  const r = e ? n.template : n.falseTemplate;
  if (!r) return;
  const s = document.createDocumentFragment();
  r.renderInto(s), n.container = s, n.nodes = Array.from(s.childNodes);
  for (const i of n.nodes) t.insertBefore(i, n.endMarker);
}
function Ce(n) {
  for (const e of n.nodes) e.parentNode?.removeChild(e);
  n.nodes = [], n.container = null;
}
function Jt(n) {
  return $((e, t) => {
    const r = e, s = /* @__PURE__ */ new Set();
    for (const [i, o] of Object.entries(n)) o && (r.classList.add(i), s.add(i));
    if (t)
      for (const i of t) s.has(i) || r.classList.remove(i);
    return s;
  });
}
function Qt(n) {
  return $((e, t) => {
    const r = e, s = /* @__PURE__ */ new Set();
    for (const [i, o] of Object.entries(n)) o !== void 0 && (r.style.setProperty(i.replace(/([A-Z])/g, "-$1").toLowerCase(), String(o)), s.add(i));
    if (t)
      for (const i of t) s.has(i) || r.style.removeProperty(i.replace(/([A-Z])/g, "-$1").toLowerCase());
    return s;
  });
}
function Zt(n) {
  return $((e, t) => {
    if (!t) {
      const r = e.parentNode;
      if (!r) throw new Error("unsafeHTML() directive: container must be attached to a parent node");
      const s = document.createComment("unsafeHTML-start"), i = document.createComment("unsafeHTML-end");
      r.replaceChild(s, e), r.insertBefore(i, s.nextSibling);
      const o = {
        html: "",
        startMarker: s,
        endMarker: i,
        nodes: []
      };
      return Re(n, o), o;
    }
    return t.html === n || Re(n, t), t;
  });
}
function Re(n, e) {
  const t = e.startMarker.parentNode;
  if (!t) throw new Error("unsafeHTML() directive: markers not in DOM");
  for (const i of e.nodes) i.parentNode?.removeChild(i);
  const r = document.createElement("div");
  r.innerHTML = n;
  const s = document.createDocumentFragment();
  for (; r.firstChild; ) s.appendChild(r.firstChild);
  e.nodes = Array.from(s.childNodes);
  for (const i of e.nodes) t.insertBefore(i, e.endMarker);
  e.html = n;
}
function bt(n) {
  return typeof n == "string" ? document.querySelector(n) : n;
}
function wt(n) {
  return typeof n == "string" ? {
    target: n,
    persist: !1
  } : n instanceof Element ? {
    target: n,
    persist: !1
  } : {
    target: n.target,
    persist: n.persist ?? !1
  };
}
function Ct(n, e, t) {
  if (!e) {
    console.warn("portal directive: value is required");
    return;
  }
  const r = wt(e), s = bt(r.target);
  if (!s) {
    console.warn(`portal directive: target "${r.target}" not found`);
    return;
  }
  if (n.parentNode === s) return;
  const i = document.createComment("portal-placeholder");
  return n.parentNode?.insertBefore(i, n), n.removeAttribute(":portal"), s.appendChild(n), () => {
    r.persist || n.remove(), i.remove();
  };
}
Oe("portal", Ct);
var Yt = class {
  constructor() {
    this.__directive = !0;
  }
};
export {
  Qe as APP_CONFIG,
  Ye as AbortError,
  H as Binding,
  ze as ComponentBase,
  Ht as ComponentStateBaseService,
  Yt as Directive,
  dt as EffectsBase,
  re as HttpBaseError,
  pe as HttpClient,
  fe as HttpError,
  Rt as Inject,
  te as Injectable,
  je as InjectionEngine,
  k as Injector,
  Me as MelodicComponent,
  Ze as NetworkError,
  tt as ROUTE_CONTEXT_EVENT,
  Y as RX_ACTION_PROVIDERS,
  ee as RX_EFFECTS_PROVIDERS,
  Z as RX_INIT_STATE,
  $e as RX_STATE_DEBUG,
  Nt as RouteContextEvent,
  X as RouteContextService,
  U as RouteMatcher,
  Q as RouterLinkComponent,
  J as RouterOutletComponent,
  A as RouterService,
  xe as SIGNAL_MARKER,
  O as Service,
  He as SignalEffect,
  N as SignalStoreService,
  ct as TemplateResult,
  Ve as applyGlobalStyles,
  xt as bootstrap,
  rt as buildPathFromRoute,
  Jt as classMap,
  E as computed,
  Bt as createAction,
  kt as createDeactivateGuard,
  St as createGuard,
  Vt as createReducer,
  At as createResolver,
  Ft as createState,
  B as createToken,
  lt as css,
  Et as defineConfig,
  $ as directive,
  Je as environment,
  nt as findRouteByName,
  Pe as getActiveEffect,
  ot as getAttributeDirective,
  Xe as getEnvironment,
  $t as getRegisteredDirectives,
  M as getTokenKey,
  Dt as hasAttributeDirective,
  oe as html,
  q as isDirective,
  Le as isSignal,
  z as matchRouteTree,
  Kt as onAction,
  Ct as portalDirective,
  Lt as props,
  Pt as provideConfig,
  Mt as provideHttp,
  Wt as provideRX,
  Oe as registerAttributeDirective,
  ae as render,
  Ut as repeat,
  zt as repeatRaw,
  ht as routerLinkDirective,
  de as setActiveEffect,
  S as signal,
  Qt as styleMap,
  Ot as unregisterAttributeDirective,
  Zt as unsafeHTML,
  Xt as when
};
